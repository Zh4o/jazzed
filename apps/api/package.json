{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "poetry run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload",
    "lint": "poetry run ruff check .",
    "format": "poetry run ruff format ."
  }
}